"""
ADP (Agent Discovery Protocol) 常量定义

定义 ADP 协议中使用的默认值、限制参数和协议常量。
基于 ACPs-spec-ADP 协议规范。
"""

# =============================================================================
# 转发深度 (Forward Depth) 相关常量
# =============================================================================

# 转发深度默认值
FORWARD_DEPTH_LIMIT_DEFAULT: int = 3

# 转发深度绝对上限
FORWARD_DEPTH_LIMIT_MAX: int = 5

# 转发深度最小值
FORWARD_DEPTH_LIMIT_MIN: int = 1

# =============================================================================
# 转发扇出 (Forward Fanout) 相关常量
# =============================================================================

# 转发扇出默认值（未提供时默认不允许并发转发）
FORWARD_FANOUT_LIMIT_DEFAULT: int = 1

# 转发扇出绝对上限
FORWARD_FANOUT_LIMIT_MAX: int = 5

# 转发扇出最小值
FORWARD_FANOUT_LIMIT_MIN: int = 1

# =============================================================================
# 超时相关常量 (毫秒)
# =============================================================================

# 单跳转发默认超时 (ms)
FORWARD_EACH_TIMEOUT_MS_DEFAULT: int = 10_000

# 转发总超时默认值 (ms)
FORWARD_TOTAL_TIMEOUT_MS_DEFAULT: int = 60_000

# =============================================================================
# 重定向相关常量
# =============================================================================

# 客户端最大连续重定向次数（建议值）
MAX_REDIRECT_HOPS: int = 5

# =============================================================================
# 过滤条件相关常量
# =============================================================================

# 过滤条件嵌套深度建议上限
FILTER_MAX_NESTING_DEPTH: int = 3

# =============================================================================
# 上下文载荷相关常量
# =============================================================================

# DiscoveryContext 建议最大载荷大小 (字节)
CONTEXT_MAX_PAYLOAD_BYTES: int = 2048

# =============================================================================
# API 路径
# =============================================================================

# 发现请求 API 路径
DISCOVER_API_PATH: str = "/discover"

# =============================================================================
# HTTP 请求/响应头字段名称
# =============================================================================

# 分布式追踪相关头（示例名称，实际使用参考监控日志规范）
HEADER_TRACE_ID: str = "X-Trace-Id"
HEADER_SPAN_ID: str = "X-Span-Id"
HEADER_PARENT_SPAN_ID: str = "X-Parent-Span-Id"

# =============================================================================
# 查询类型常量
# =============================================================================

# 明确查询（默认值）
QUERY_TYPE_EXPLICIT: str = "explicit"

# 探索性查询
QUERY_TYPE_EXPLORATORY: str = "exploratory"

# 热门查询
QUERY_TYPE_TRENDING: str = "trending"

# 过滤查询
QUERY_TYPE_FILTERED: str = "filtered"

# 所有合法查询类型
QUERY_TYPES: tuple = (
    QUERY_TYPE_EXPLICIT,
    QUERY_TYPE_EXPLORATORY,
    QUERY_TYPE_TRENDING,
    QUERY_TYPE_FILTERED,
)

# =============================================================================
# 路由状态常量
# =============================================================================

ROUTE_STATUS_OK: str = "ok"
ROUTE_STATUS_TIMEOUT: str = "timeout"
ROUTE_STATUS_ERROR: str = "error"

# =============================================================================
# 过滤逻辑运算符常量
# =============================================================================

FILTER_LOGIC_AND: str = "and"
FILTER_LOGIC_OR: str = "or"
FILTER_LOGIC_NOT: str = "not"
