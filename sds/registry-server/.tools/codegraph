#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"
export PYTHONPATH="$ROOT/.py312deps:$ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m codegraph.main "$@"
