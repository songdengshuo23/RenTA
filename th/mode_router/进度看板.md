# 多智能体编排进度看板

更新时间：2026-06-08

## 已完成
- 已接入多智能体群聊式协作框架，保留 `mode_router` 作为编排层。
- 已把文献综述流程的执行链路接入现有 `th/mode_router`。
- 已补充运行过程中的状态记录与数据流产物。
- 已完成相关文件的备份与语法检查。
- 已完成运行状态展示增强：`/workflow/latest` 新增 `progress`、`current`、`role_progress`、`stage_progress`、`operator_summary`，`/workflow/monitor` 增加整体进度、角色进度卡片、阶段清单和事件流。
- 已补充运行产物：`12_state_timeline.md` 展开完整状态时间线，`13_run_status_summary.json` / `13_run_status_summary.md` 保存机器可读与人可读的运行摘要。
- 已修复文献综述工作流中损坏的中文 prompt / 汇总文本，提升任务分配和结果汇总可读性。
- 已完成远端验证：`python3 -m unittest discover -s tests -v` 通过 25 个测试；模拟端到端运行目录为 `literature_runs/20260603_180118_验证新版运行状态展示与角色进度摘要`，状态摘要显示 13/13 步完成、3 个角色齐全。
- 已重启远端服务：`http://127.0.0.1:18080/health` 正常，`/workflow/latest` 返回 100% 进度，`/workflow/monitor` 返回 HTTP 200。
- 已在远端自行启动 RabbitMQ：`cb-orchestrator-rabbitmq` 容器运行中，`5672` 与 `15672` 端口已开放映射。
- 已按真实群聊总线链路完成非模拟验证：`literature_runs/20260603_192139_真实_RabbitMQ_群聊总线链路验证_围绕_agent_harness_的作`，状态为 `done`，进度 13/13（100%），`search` / `analysis` / `writing` 三个角色均由实际 partner agent 返回结果，最终写作结果长度 8844 字符。
- 已针对真实验证后状态再次回归：`python3 -m unittest discover -s tests -v` 通过 25 个测试；`/workflow/latest` 指向最新真实 RabbitMQ 运行，`/workflow/monitor` 返回 HTTP 200。
- 已完成 Registry Passport 与 Orchestrator 编排端基础对接：新增 Registry client，支持 `/passports/discovery` 候选池接入、`/passports/{agent_aic}/dispatch` 派发前准入、`/passports/runtime-review/schedule` 运行期复审调度代理；`/pipeline/registry` 可将 Passport 摘要归一化为现有候选 skill 并生成带 `dispatch_guard` 的编排计划。
- 已补充 Registry 对接验证：本地与远端 `python3 -m unittest discover -s tests -v` 均通过 27 个测试；`python3 -m compileall adapters.py registry_client.py orchestrator.py service.py tests` 通过。
- 已完成真实 Registry HTTP 联调与部署确认：远端源代码挂载 Registry 容器 `sds-registry-passport-source` 运行在 `18002`，OpenAPI 已确认包含 `/passports/discovery`、`/passports/{agent_aic}/dispatch`、`/passports/runtime-review/schedule` 三条 Passport 路由。
- 已完成真实 Registry 业务链路验证：`/passports/discovery` 返回 1 个 `VALID` / `APPROVE` / `T3` Passport；`/passports/{agent_aic}/dispatch` 返回 `eligibleForDispatch=true` 且 `reasons=[]`；`/registry/runtime-review/schedule` 经 Orchestrator 代理调用真实 Registry 返回 HTTP 200。
- 已完成真实 Orchestrator -> Registry 通用编排验证：使用非文献综述的“企业内部知识库迁移”任务调用 `/pipeline/registry`，返回 `source=registry_pipeline`、`normalized_skill_count=1`、`decision_mode=mode_2`、`plan_status=planned`、`registry_validation.status=passed`、`checked_agents=1`、`dispatch_guard.eligible_for_dispatch=true`，说明对接位于通用编排层而非文献综述实例流程。
- 已同步远端主线修正：`registry_validation.checked_agents` 现在按唯一 AIC 去重，避免同一 agent 同时出现在 primary / work package 时重复计数；远端服务已重启，`http://127.0.0.1:18080/health` 正常。
- 部署说明：当前 Registry 源容器使用 `CA_SERVER_MOCK=true`，仅用于绕过 CA 服务侧尚缺少的 `/ca/passport-sync` 证书同步接口；本轮 Passport 路由、Registry 数据库、鉴权、发现、派发准入、复审调度和 Orchestrator HTTP 调用均为真实联调。
- 已按 `模式路由.md` 接入三档模式路由：新增 `route_classifier.py`，支持 `LLM` -> `mode_0` 直答、`Agent` -> `mode_1` 单智能体工具链、`多Agent` -> `mode_2` 多智能体编排；新增 `/mode/classify`，并将 `/pipeline/registry` 改为先做三档路由再接 Registry Passport 候选与 dispatch guard。
- 已完成远端 Orchestrator 回归：本地与远端 `python3 -m unittest discover -s tests -v` 均通过 34 个测试；远端 `python3 -m compileall route_classifier.py mode_selector.py orchestrator.py service.py tests` 通过；远端服务已重启，`http://127.0.0.1:18080/health` 正常。
- 已完成 Registry 上传 -> Supervisor 审查 -> Passport -> 管理员审批 -> ATR discovery 的真实业务流：用户上传 `Full Flow Safe Text Agent 20260607222355`，Supervisor 返回 `APPROVE` / `LOW` / `T3`，Passport 为 `VALID`，管理员审批后 Agent AIC 为 `1.2.156.3088.0001.00001.MB8XX8.RU9HJW.1.0MTG`，`/passports/discovery` 可发现，`/passports/{agent_aic}/dispatch` 返回 `eligibleForDispatch=true` 且 `reasons=[]`。
- 已完成三模式全流程验证：`LLM` 用 `/pipeline/registry` 返回 `mode_0`、`llm_direct_answer`、无 dispatch；`Agent` 返回 `mode_1`、`single_agent_multi_skill`、`registry_validation.status=passed`、`dispatch_guard.eligible_for_dispatch=true`；`多Agent` 返回 `mode_2`、`tree_root_parallel_children`、`registry_validation.status=passed`、两个 dispatch view 均通过，且包含本轮真实上传审批后的 Agent。
- 已完成 Registry 关键测试回归：`PYTHONPATH=.py312deps python3 -m pytest tests/test_atr_api.py tests/test_supervisor.py -q` 通过 47 个测试。
- 已完成 Registry 状态机修正：Supervisor `APPROVE` 且 Passport 为 `VALID` 时自动发布 Agent，直接置为 `APPROVED`、生成 AIC、同步 ACS，并把 Review / Passport / certificate 中的 AIC 统一为 Agent 主记录 AIC；`MANUAL_REVIEW` 初始保持 `PENDING`，staff/admin 人工 approve 后会把最新 `DRAFT` / `MANUAL_REVIEW` Passport 提升为 `VALID` / `APPROVE` 并重新同步 identity / certificate AIC；`REJECT` / `ERROR` 不公开。
- 已收紧 ATR Discovery 发布条件：`/passports/discovery` 现在只发布 `VALID` / `APPROVE` 且 Agent 已 `APPROVED`、有 AIC、active、未删除、未禁用的记录，避免历史 `VALID` Passport 但 Agent 未发布时进入可发现索引。
- 已完成自动发布真实 HTTP 全链路验证：注册临时 CLIENT、创建并提交 `Auto Publish Safe Agent 2026060808****` 后，提交响应直接返回 `approval_status=APPROVED` 且有 AIC；最新 Passport 为 `VALID` / `APPROVE` / `LOW` / `T3`，Passport identity AIC 与 certificate AIC 均匹配 Agent 主记录；ATR Discovery 命中新 Agent 且 `nullAicCount=0`；ATR Dispatch 返回 `eligibleForDispatch=true`、`reasons=[]`。验证 AIC：`1.2.156.3088.0001.00001.L2PQ7L.2TAVD3.1.0OK9`。
- 已补充 “LLM 审核或人工审核通过，一个通过即可可发现” 覆盖：规则层 warning 但 LLM Judge 返回 `LOW` 且无 overclaim 时，Supervisor 决策收口为 `APPROVE` 并自动发布；`MANUAL_REVIEW` 经 staff 人工 approve 后也发布。两条路径的 Passport 均为 `VALID` / `APPROVE`，ATR Discovery 可发现，ATR Dispatch `eligibleForDispatch=true` 且 `reasons=[]`。
- 已完成人工审核发布真实 HTTP 联调：容器内通过真实 `/api/auth/login`、`/api/agent/client`、`/api/agent/client/{agent_id}/submit`、`/api/agent/staff/{agent_id}/process`、`/acps-atr-v2/passports/discovery`、`/acps-atr-v2/passports/{agent_aic}/dispatch` 跑通；自动 `APPROVE` 样本和人工 approve 样本均可发现、可 dispatch。
- 已完成本轮远端回归：Registry `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=.py312deps python3 -m pytest tests/test_atr_api.py tests/test_supervisor.py -q` 通过 47 个测试；`sds-registry-passport-source` 已重启并确认加载自动发布、人工发布与 Discovery 过滤代码；Orchestrator 远端 `python3 -m pytest -q` 通过 34 个测试，`/mode/classify` HTTP 探活正常。
- 已将模式路由 LLM 切换到 DeepSeek：远端 `mode_router/.env` 已配置 `ROUTER_LLM_URL=https://api.deepseek.com`、`ROUTER_LLM_MODEL=deepseek-chat` 与 `ROUTER_LLM_API_KEY`，权限为 `600`；新增 `start_service.sh` 并用其重启 `18080` 服务，确保运行时加载真实 LLM 配置。
- 已完成 DeepSeek 模式路由真实验证：`/mode/classify` 对直答、单 Agent、 多 Agent 三类任务均返回 `llm_used=true`、`model=deepseek-chat`，分别命中 `mode_0` / `mode_1` / `mode_2`；`/pipeline/registry` 也返回 `llm_used=true`、`model=deepseek-chat`，说明 Registry 编排入口已使用真实 LLM 路由。
- 已补齐 Orchestrator 到 Registry 的服务 token 环境：从 Registry 源容器环境脱敏同步 `REGISTRY_SERVICE_TOKEN` / `ORCHESTRATOR_REGISTRY_SERVICE_TOKEN` 到 `mode_router/.env`，`/pipeline/registry` 不再因缺失 bearer token 阻断 Registry 调用。
- 已完成 DeepSeek 部署后回归：远端 `python3 -m unittest discover -s tests -v` 通过 34 个测试，`http://127.0.0.1:18080/health` 返回正常。

## 待办
- 根据后续 demo 反馈再做一轮交互和日志优化。

## 关键文件
- `literature_workflow.py`
- `adapters.py`
- `registry_client.py`
- `route_classifier.py`
- `模式路由.md`
- `orchestrator.py`
- `service.py`
- `tests/test_route_classifier.py`
- `tests/test_registry_pipeline.py`
- `run_demo.sh`
- `literature_runs/<run_id>/08e_workflow_checklist.json`
- `literature_runs/<run_id>/09_full_data_flow.json`
- `literature_runs/<run_id>/12_state_events.jsonl`
- `literature_runs/<run_id>/12_state_timeline.md`
- `literature_runs/<run_id>/13_run_status_summary.json`
- `literature_runs/<run_id>/13_run_status_summary.md`
